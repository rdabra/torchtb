@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Public dependencies your targets link PUBLIC to:
# (match what you used in target_link_libraries(... PUBLIC ...))
find_dependency(Arrow  CONFIG)
find_dependency(Parquet CONFIG)
find_dependency(Torch  CONFIG)    # if you link to Torch PUBLIC

# Optional: expose useful variables to consumers
set(TORCHTB_VERSION        "@PROJECT_VERSION@")
set(TORCHTB_INCLUDE_DIR    "${PACKAGE_PREFIX_DIR}/@CMAKE_INSTALL_INCLUDEDIR@")

# Bring in the imported targets (torchtb::torchtb_shared, torchtb::torchtb_static, etc.)
include("${CMAKE_CURRENT_LIST_DIR}/torchtbTargets.cmake")
